# Copy this file to .env and fill in your own keys
# NEVER commit the .env file to version control

# ── Direct mode (default, local dev only) ─────────────────────────────────
# API key is loaded into browser bundle — acceptable only for local personal use.
GEMINI_API_KEY=your_gemini_api_key_here
VITE_GOOGLE_API_KEY=your_gemini_api_key_here

# ── Proxy mode (recommended for any deployment) ────────────────────────────
# Set VITE_USE_PROXY=true and keep GOOGLE_API_KEY only on the backend server.
# Frontend never sees the real key.
# VITE_USE_PROXY=true
# VITE_BACKEND_URL=http://localhost:8000

# Backend (Python FastAPI) — only needed for proxy mode / ChromaDB style
GOOGLE_API_KEY=your_gemini_api_key_here

# ── Optional: Supabase history persistence ─────────────────────────────────
# SUPABASE_URL=your_supabase_url_here
# SUPABASE_KEY=your_supabase_anon_key_here
